<CascadingValue Value="this">
    <div id="@Id">
        <!--LINKS PARA NAVEGAR POR CADA PAGINA-->
        <!--<ul class="nav nav-pills nav-justified">
        @foreach (var step in Steps)
        {
            <li id="step-@(StepsIndex(step)+1)" class="nav-item">
                <a class="nav-link @((ActiveStep == step) ? "active" : "")" href="javascript: void(0)"
                   @onclick="@(e=> SetActive(step))">@step.Name</a>
            </li>
        }
        </ul>-->
        <div id="container-fluid">
            @ChildContent
        </div>
    </div>
    <div class="btn-group-sm p-1 mt-5">
        <div class="float-left">
            <button class="btn btn-primary btn-lg" type="button" disabled="@(ActiveStepIx==0)" @onclick="GoBack">Anterior</button>
        </div>
        <div class="float-right">
            <button class="btn btn-primary btn-lg" type="@(IsLastStep ? "submit" : "button")" @onclick="GoNext">@(IsLastStep ? "Finalizar" : "Siguiente")</button>
        </div>
    </div>
</CascadingValue>
